'use strict';
// Symbol is needed in `for of` and destructuring ops
require('./symbol');
// Array.from is needed in `for of`
require('./array-from');

require('./object-assign');

require('./promise');
